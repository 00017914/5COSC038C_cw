# AI Usage Statement

## AI was used for

- planning the overall Streamlit app structure
- drafting Python code for data cleaning, visualization, and export features
- drafting repository documentation

## Manual verification completed

- checked the coursework brief against the implemented features in `app.py`
- verified that the app includes the four required pages
- verified that the app supports CSV, XLSX, and JSON upload paths in code
- verified that the transformation log, undo, reset, export, and recipe download are implemented
- verified that two sample datasets are included in `sample_data/`
- verified that the app includes the required visualization types using matplotlib

## Manual work still required before submission

- install dependencies and run the app locally
- record the 3 to 5 minute demo video
- deploy the app to Streamlit Community Cloud and add the live URL
- write the final 2-page team report manually without AI assistance

## Risk awareness

AI-generated code can be imperfect. The full app should be tested against the lecturer's marking rubric before submission, especially with Excel files, large datasets, and edge-case cleaning actions.
